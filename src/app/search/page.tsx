import Link from "next/link";import {prisma} from "@/lib/prisma";import {PageFrame} from "@/components/SiteShell";
export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const q=(await searchParams).q?.trim()||"";const [plays,people,theatres,posts]=q?await Promise.all([prisma.play.findMany({where:{status:"PUBLISHED",OR:[{title:{contains:q,mode:"insensitive"}},{description:{contains:q,mode:"insensitive"}}]},take:20}),prisma.profile.findMany({where:{OR:[{name:{contains:q,mode:"insensitive"}},{bio:{contains:q,mode:"insensitive"}}]},take:20}),prisma.theatre.findMany({where:{OR:[{title:{contains:q,mode:"insensitive"}},{about:{contains:q,mode:"insensitive"}}]},take:20}),prisma.blogPost.findMany({where:{status:"PUBLISHED",OR:[{title:{contains:q,mode:"insensitive"}},{content:{contains:q,mode:"insensitive"}}]},take:20})]):[[],[],[],[]];const items=[...plays.map(x=>({title:x.title,url:`/play/${x.slug}/`})),...people.map(x=>({title:x.name,url:`/profile/${x.slug}/`})),...theatres.map(x=>({title:x.title,url:`/theatre/${x.slug}/`})),...posts.map(x=>({title:x.title,url:`/blog/${x.slug}/`}))];return <><header className="site-container"><h1>Search Results</h1></header><PageFrame><form><input className="input" name="q" defaultValue={q}/><button className="button">Search</button></form><p>{items.length?`${items.length} results matching your query: ${q}`:`No results were found matching your query: ${q}`}</p>{items.map(i=><h5 key={i.url}><Link href={i.url}>{i.title}</Link></h5>)}</PageFrame></>}
