import {notFound} from "next/navigation";import {prisma} from "@/lib/prisma";import {publishedWhere} from "@/lib/content";import {PageFrame} from "@/components/SiteShell";
export default async function Shows({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const play=await prisma.play.findFirst({where:{slug,...publishedWhere()},include:{shows:{orderBy:{showtime:"desc"},include:{theatre:true}}}});if(!play)notFound();return <><header className="site-container"><h1>{play.title}</h1></header><PageFrame>{play.shows.length?<ul>{play.shows.map(s=><li key={s.id}>{new Intl.DateTimeFormat("en-NP",{dateStyle:"full",timeStyle:"short",timeZone:"Asia/Kathmandu"}).format(s.showtime)} — {s.theatre.title}</li>)}</ul>:<p>No shows yet.</p>}</PageFrame></>}
