import Link from "next/link";import {redirect} from "next/navigation";import {currentUser,clearSession} from "@/lib/auth";import {prisma} from "@/lib/prisma";
async function logout(){"use server";await clearSession();redirect("/")}
export default async function Admin(){const user=await currentUser();if(!user?.isStaff)redirect("/admin/login/");const [plays,profiles,theatres,schedules,posts,entries]=await Promise.all([prisma.play.count(),prisma.profile.count(),prisma.theatre.count(),prisma.showsMeta.count(),prisma.blogPost.count(),prisma.formEntry.count()]);return <div className="site-container"><h1>Site administration</h1><p>Welcome, {user.username}. <form action={logout} style={{display:"inline"}}><button>Log out</button></form></p><table className="admin-table"><thead><tr><th>Content</th><th>Count</th><th>Manage</th></tr></thead><tbody>{[["Plays",plays,"plays"],["Profiles",profiles,"profiles"],["Theatres",theatres,"theatres"],["Show schedules",schedules,"schedules"],["Blog posts",posts,"posts"],["Form entries",entries,"entries"]].map(([n,c,k])=><tr key={String(k)}><td>{n}</td><td>{c}</td><td><Link href={`/admin/${k}/`}>View</Link></td></tr>)}</tbody></table></div>}
