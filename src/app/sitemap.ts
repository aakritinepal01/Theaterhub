import type {MetadataRoute} from "next";import {prisma} from "@/lib/prisma";
export const dynamic = "force-dynamic";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const [plays,profiles,theatres,posts,pages]=await Promise.all([prisma.play.findMany({where:{status:"PUBLISHED",inSitemap:true},select:{slug:true,updated:true}}),prisma.profile.findMany({where:{status:"PUBLISHED",inSitemap:true},select:{slug:true,updated:true}}),prisma.theatre.findMany({where:{status:"PUBLISHED",inSitemap:true},select:{slug:true,updated:true}}),prisma.blogPost.findMany({where:{status:"PUBLISHED",inSitemap:true},select:{slug:true,updated:true}}),prisma.page.findMany({where:{status:"PUBLISHED",inSitemap:true},select:{slug:true,updated:true}})]);return [{url:base,lastModified:new Date()},...plays.map(x=>({url:`${base}/play/${x.slug}/`,lastModified:x.updated||undefined})),...profiles.map(x=>({url:`${base}/profile/${x.slug}/`,lastModified:x.updated||undefined})),...theatres.map(x=>({url:`${base}/theatre/${x.slug}/`,lastModified:x.updated||undefined})),...posts.map(x=>({url:`${base}/blog/${x.slug}/`,lastModified:x.updated||undefined})),...pages.map(x=>({url:`${base}/${x.slug}/`,lastModified:x.updated||undefined}))]}
